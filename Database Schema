Users
	Username(CharField) Primary Key
	Email(CharField)
	EncryptedPassword

UserPalace
	Username(charset) Unique foreign Key
	PalaceName(CharField) Unique Key
	Public(Boolean)
	NumberOfRooms(int)

PalaceRoom
	Username(CharField) Unique Foreign Key
	PalaceName(CharField) Unique Foreign Key
	RoomName Unique(CharField) Unique Key
	BackgroundImage(Image)

PalaceObjects
	Username(CharField) Unique foreign Key
	PalaceName(CharField) Unique Forign Key
	ObjectName(CharField) Unique key
	ObjectImage(image)
	FileLocation(Coordinates on page)
	Description(CharField)
